[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "ecochronos-vault"
version = "0.1.0"
description = "Open archive for high-resolution microclimate and surface ecology time series"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.32,<1",
    "pydantic-settings>=2.6,<3",
    "psycopg[binary]>=3.2,<4",
    "minio>=7.2,<8",
    "httpx>=0.27,<1",
    "apscheduler>=3.10,<4",
    "duckdb>=1.1,<2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3,<9",
]

[project.scripts]
ecochronos-vault = "ecochronos_vault.main:main"
ecochronos-vault-batch = "ecochronos_vault.batch:main"
ecochronos-vault-migrate = "ecochronos_vault.migrate:main"

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
ecochronos_vault = ["sql/*.sql"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
