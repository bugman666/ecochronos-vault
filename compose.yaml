name: ecochronos-vault

services:
  api:
    build: .
    environment:
      API_HOST: 0.0.0.0
      API_PORT: 8000
      LOG_LEVEL: ${LOG_LEVEL:-info}
      POSTGRES_DSN: postgresql://${POSTGRES_USER:-ecochronos}:${POSTGRES_PASSWORD:-ecochronos_local_dev}@postgres:5432/${POSTGRES_DB:-ecochronos}
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-ecochronos}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-ecochronos_local_dev_minio}
      MINIO_BUCKET: ${MINIO_BUCKET:-ecochronos}
      MINIO_SECURE: "false"
      ARCHIVE_UPLOAD_TOKEN: ${ARCHIVE_UPLOAD_TOKEN:-}
    ports:
      - "${API_PORT:-8000}:8000"
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/healthz', timeout=2)",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - ecochronos
    restart: unless-stopped

  postgres:
    image: postgis/postgis:16-3.5-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ecochronos}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ecochronos_local_dev}
      POSTGRES_DB: ${POSTGRES_DB:-ecochronos}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - ecochronos
    restart: unless-stopped

  # Quay still publishes MinIO images; Docker Hub minio/minio is no longer publicly pullable.
  minio:
    image: quay.io/minio/minio:RELEASE.2025-09-07T16-13-09Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-ecochronos}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-ecochronos_local_dev_minio}
    volumes:
      - minio_data:/data
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    healthcheck:
      # Server image has no `mc`; use the built-in live endpoint.
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9000/minio/health/live"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - ecochronos
    restart: unless-stopped

  minio-init:
    image: quay.io/minio/mc:RELEASE.2025-08-13T08-35-41Z
    depends_on:
      minio:
        condition: service_healthy
    environment:
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-ecochronos}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-ecochronos_local_dev_minio}
      MINIO_BUCKET: ${MINIO_BUCKET:-ecochronos}
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        mc alias set local http://minio:9000 "$$MINIO_ACCESS_KEY" "$$MINIO_SECRET_KEY"
        mc mb --ignore-existing "local/$$MINIO_BUCKET"
    networks:
      - ecochronos
    restart: on-failure

networks:
  ecochronos:
    driver: bridge

volumes:
  postgres_data:
  minio_data:
